<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light">
        <!-- Customize your theme here. -->
        <item name="android:buttonStyle">@style/Button</item>
        <item name="android:editTextStyle">@style/EditText</item>
        <item name="android:actionBarStyle">@style/ActionBar</item>
        <item name="android:actionBarTabStyle">@style/Tab</item>
        <item name="android:actionBarTabTextStyle">@style/TabText</item>
    </style>

    <style name="Button">
        <item name="android:background">@android:drawable/editbox_background</item>
        <item name="android:height">48dp</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="EditText" parent="android:style/Widget.EditText">
        <item name="android:background">@drawable/edittext_selector</item>
    </style>

    <style name="ActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:titleTextStyle">@style/ActionBarText</item>
        <item name="android:background">@color/pomegranate</item>
        <item name="android:icon">@drawable/ic_launcher_white</item>
    </style>

    <style name="ActionBarText" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:textSize">16dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Tab">
        <item name="android:background">@drawable/tab_background</item>
        <item name="android:gravity">center</item>
        <item name="android:height">20dp</item>
    </style>

    <style name="TabText">
        <item name="android:textSize">14dp</item>
        <item name="android:textColor">@color/pomegranate</item>
    </style>

</resources>
